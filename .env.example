# Discord Webhook & Target Channel Configuration
# (Create a Webhook in Discord Channel Settings -> Integrations -> Webhooks)
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN"
DISCORD_CHANNEL_NAME="school-announcements"

# Support Contact
NEXT_PUBLIC_SUPPORT_EMAIL="tchen117@uottawa.ca"

# Whitelist of authorized faculty/admin email addresses (comma-separated, case-insensitive)
ADMIN_WHITELIST="tchen117@uottawa.ca"

# Secret key for JWT session encryption (generate a random 32+ char string)
AUTH_SECRET="syncbridge_super_secret_jwt_key_change_in_production"

# Email Provider: Resend (Optional for local simulation dev mode)
RESEND_API_KEY=""
EMAIL_FROM="uOttawa MIAI SyncBridge <auth@orbitalassets.net>"
